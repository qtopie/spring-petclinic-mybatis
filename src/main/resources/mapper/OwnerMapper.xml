<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springframework.samples.petclinic.mapper.OwnerMapper">
    <resultMap id="owners" type="org.springframework.samples.petclinic.model.Owner">
        <id column="id" property="id" />
        <result column="first_name" property="firstName" />
        <result column="last_name" property="lastName" />
    </resultMap>
    
    <select id="findByLastName" resultMap="owners">
    	<bind name="pattern" value="'%' + _parameter + '%'" />
        select * from owners where last_name like #{pattern}
    </select>
    
    <insert id="save" useGeneratedKeys="true" keyProperty="id" 
    	parameterType="org.springframework.samples.petclinic.model.Owner">
    	INSERT INTO owners
    		(id,
    		first_name,
    		last_name,
    		address,
    		city,
    		telephone) 
    	VALUES(
	    	#{id}, 
	    	#{firstName}, 
	    	#{lastName},
	    	#{address}, 
	    	#{city}, 
	    	#{telephone}
    		)
    	ON DUPLICATE KEY UPDATE 
    		first_name=#{firstName},
    		last_name=#{lastName},
    		address=#{address},
    		city=#{city},
    		telephone=#{telephone};
    </insert>
</mapper>