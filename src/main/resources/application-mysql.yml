# uncomment for init database (first start)
spring:
  datasource:
    initialization-mode: always
    # MySQL config start
    #----------------------------------------------------------------
    # allow multi queries or use java 8 default method to execute multiple sql
    url: jdbc:mysql://${dbHost:127.0.0.1}:3306/petclinic?useUnicode=true&allowMultiQueries=true
    username: root
    password: petclinic
    #----------------------------------------------------------------
    # MySQL config end 
